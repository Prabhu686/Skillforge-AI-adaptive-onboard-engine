# ── Server ──────────────────────────────────────────────
PORT=5000
CLIENT_URL=http://localhost:5173

# Optional – omit to use O*NET taxonomy keyword fallback
OPENAI_API_KEY=sk-...

# ── Client (Vite) ────────────────────────────────────────
# No client-side secrets required.
# API calls are proxied through Vite → http://localhost:5000
